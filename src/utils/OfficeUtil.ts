import {    excelMimeTypes,    officeMimeTypes,    pdfMimeTypes,    pptMimeTypes,    wordMimeTypes} from "../constants/FileTypeConstatnts.ts";/** * 判断文件是否是Word文件 * @param filePath 文件路径 */export function isWord(filePath: string) {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return wordMimeTypes[suffix] !== undefined;}/** * 判断文件是否是excel文件 * @param filePath 文件路径 */export function isExcel(filePath: string) {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return excelMimeTypes[suffix] !== undefined;}/** * 判断文件是否是ppt文件 * @param filePath 文件路径 */export function isPPT(filePath: string) {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return pptMimeTypes[suffix] !== undefined;}/** * 判断文件是否是pdf文件 * @param filePath 文件路径 */export function isPDF(filePath: string) {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return pdfMimeTypes[suffix] !== undefined;}/** * 判断文件是否是office文件 * @param filePath 文件路径 */export function isOffice(filePath: string) {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return officeMimeTypes[suffix] !== undefined;}