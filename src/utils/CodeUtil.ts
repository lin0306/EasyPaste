import {codeFileTypes} from "../constants/FileTypeConstatnts.ts";/** * 判断文件是否是程序 * @param filePath 文件路径 */export function isCode(filePath: string) {    const ext = filePath.split('.').pop() || '';    return codeFileTypes[ext] !== undefined || false;}/** * 获取代码高亮语言 * @param filePath 文件路径 */export function getCodeLanguage(filePath: string) {    const ext = filePath.split('.').pop() || '';    return codeFileTypes[ext] !== undefined ? ext : '';}/** * 获取文件类型 * @param filePath 文件路径 */export function getCodeType(filePath: string) {    const ext = filePath.split('.').pop() || '';    return codeFileTypes[ext] || '';}