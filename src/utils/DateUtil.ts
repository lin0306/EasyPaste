import {currentLanguage} from "../services/LanguageService.ts";/** * 获取当前时间，格式：YYYY-MM-DD HH:mm:ss */export function getCurrentTime() {    const now = new Date();    const year = now.getFullYear();    const month = now.getMonth() + 1; // 月份从0开始，所以需要加1    const day = now.getDate();    const hours = now.getHours();    const minutes = now.getMinutes();    const seconds = now.getSeconds();    return `${year}-${month.toString().padStart(2, '0')}-${day.toString().padStart(2, '0')} ${hours.toString().padStart(2, '0')}:${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;}/** * 将秒数转换为时分秒格式 * <pre> * 10 -> 00:10 * 100 -> 01:40 * </pre> * @param seconds 秒数 */export function convertSecondsToTime(seconds: number) {    const min = Math.floor(seconds / 60);    const sec = Math.floor(seconds % 60);    return `${min.toString().padStart(2, '0')}:${sec.toString().padStart(2, '0')}`;}/** * 时间格式转换 * <pre> *     10秒前 *     1分钟前 *     1小时前 *     1天前 * </pre> * @param date 时间戳 */export function getTimeAgo(date: number) {    const seconds = Math.floor((Date.now() - date) / 1000);    if (seconds < 60) return currentLanguage.value.pages.list.justNow;    const minutes = Math.floor(seconds / 60);    if (minutes < 60) return `${minutes} ${currentLanguage.value.pages.list.minutesAgo}`;    const hours = Math.floor(minutes / 60);    if (hours < 24) return `${hours} ${currentLanguage.value.pages.list.hoursAgo}`;    const days = Math.floor(hours / 24);    return `${days} ${currentLanguage.value.pages.list.daysAgo}`;}