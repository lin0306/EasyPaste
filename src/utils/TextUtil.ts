/** * 常见音频格式的MIME类型映射 */const mimeTypes: Record<string, string> = {    'txt': 'text/plain',    'text': 'text/plain',    'log': 'text/plain',    'csv': 'text/csv',    'tsv': 'text/tab-separated-values',    'env': 'text/plain',    'dockerignore': 'text/plain',    'gitignore': 'text/plain',    'conf': 'text/plain',    'config': 'text/plain',    'rtf': 'text/rtf',    'md': 'text/markdown',    'markdown': 'text/markdown',}/** * 判断文件是否是文本文件 * @param filePath 文件路径 */export function isText(filePath: string) {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return mimeTypes[suffix] !== undefined;}/** * 文件类型是否为纯文本 * @param filePath 文件路径 */export function isPlainText(filePath: string) {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return mimeTypes[suffix] === 'text/plain' || mimeTypes[suffix] === 'text/tab-separated-values';}/** * 判断文件是否是Markdown文件 * @param filePath 文件路径 */export function isMarkdown(filePath: string) {    return filePath.toLowerCase().endsWith('.md') || filePath.toLowerCase().endsWith('.markdown');}/** * 判断文件是否是RTF文件 * @param filePath 文件路径 */export function isRTF(filePath: string) {    return filePath.toLowerCase().endsWith('.rtf');}/** * 获取文件类型 * @param filePath 文件路径 */export function getType(filePath: string) {    const ext = filePath.split('.').pop() || '';    return mimeTypes[ext] || '';}