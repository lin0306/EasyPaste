import {nextTick} from "vue";/** * 滚动到具有指定 ID 的元素 * @param {string} divId - 目标 div 的 ID * @param {number} [delay=0] - 延迟时间（毫秒） */export async function scrollToDiv(divId: any, delay: number = 0) {    // 等待 Vue 更新 DOM    await nextTick();    const element = document.getElementById(divId);    if (element) {        // 滚动到元素        setTimeout(() => {            element.scrollIntoView({behavior: 'smooth', block: 'start'});        }, delay)    } else {        console.warn(`未找到 ID 为 ${divId} 的元素`);    }}