/** * 常见音频格式的MIME类型映射 */const mimeTypes: Record<string, string> = {    'mp3': 'audio/mpeg',    'wav': 'audio/x-wav',    'aac': 'audio/aac',    'flac': 'audio/flac',    'ogg': 'audio/ogg',    'wma': 'audio/x-ms-wma',    'aif': 'audio/x-aiff',    'aiff': 'audio/x-aiff',    'mid': 'audio/x-midi',    'midi': 'audio/x-midi',    'amr': 'audio/amr',    'm4a': 'audio/m4a',    'opus': 'audio/opus',}/** * 判断文件是否是音频文件 * @param filePath 文件路径 */export function isAudio(filePath: string) {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return mimeTypes[suffix] !== undefined;}/** * 根据文件路径获取MIME类型 * @param { string } filePath 文件路径 * @returns  { string } MIME类型 */export function convertAudioMimeType(filePath: string): string {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return mimeTypes[suffix] || 'application/octet-stream';}