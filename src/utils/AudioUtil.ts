import {audioMimeTypes} from "../constants/FileTypeConstatnts.ts";/** * 判断文件是否是音频文件 * @param filePath 文件路径 */export function isAudio(filePath: string) {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return audioMimeTypes[suffix] !== undefined;}/** * 根据文件路径获取MIME类型 * @param { string } filePath 文件路径 * @returns  { string } MIME类型 */export function getType(filePath: string): string {    const suffix = filePath.toLowerCase().split('.').pop() || '';    return audioMimeTypes[suffix] || 'application/octet-stream';}