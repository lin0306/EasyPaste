import {TreeOption} from "naive-ui";import {h} from 'vue';import OpenFolderIcon from "../assets/icons/OpenFolderIcon.vue";import CloseFolderIcon from "../assets/icons/CloseFolderIcon.vue";/** * 修改展开收起时的前缀图标 */export function updatePrefixWithExpanded(    _keys: Array<string | number>,    _option: Array<TreeOption | null>,    meta: {        node: TreeOption | null        action: 'expand' | 'collapse' | 'filter'    }) {    if (!meta.node)        return    switch (meta.action) {        case 'expand':            meta.node.prefix = () =>                h(OpenFolderIcon, null, {                    default: () => h(OpenFolderIcon)                })            break        case 'collapse':            meta.node.prefix = () =>                h(CloseFolderIcon, null, {                    default: () => h(CloseFolderIcon)                })            break    }}