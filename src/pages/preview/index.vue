<script setup lang="ts">import {destroyFileView, filePath, initFileView} from "./composables/FileViewDataComposable.ts";import TitleBar from "../../components/TitleBar.vue";import {useLanguage} from "../../services/LanguageService.ts";import {onMounted, onUnmounted} from "vue";const {currentLanguage} = useLanguage();onMounted(async () => {  await initFileView();})onUnmounted(() => {  destroyFileView();})</script><template>  <TitleBar :title="currentLanguage.pages.preview.title"            :showCloseBtn="true"            :dev-tool="`preview`"/>  <div>    <div>{{ currentLanguage.pages.preview.filePathLabel }} {{ filePath }}</div>    <div>打开文件所在位置</div>  </div>  <div>  </div></template><style scoped></style>