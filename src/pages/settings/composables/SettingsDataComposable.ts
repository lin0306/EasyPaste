import {reactive, ref} from "vue";import {SETTINGS} from "../../../constants/UserSettingsConstant.ts";// 当前选择的菜单export const selectedKey = ref<string>('general');// 是否触发加载动画export const onLoading = ref(false);// 配置相关export const originalConfig = reactive<Settings>({    theme: SETTINGS.THEME.DEFAULT_THEME,    powerOnSelfStart: false,    replaceGlobalHotkey: false,    languages: SETTINGS.LANGUAGE.DEFAULT_LANGUAGE,    maxHistoryItems: 2000,    dataRetentionDays: 30,    autoCheckUpdate: true,    updateMode: SETTINGS.UPDATER.CHECK_MODE.TIMING,    autoCheckUpdateInterval: 1,    enableTag: true,    bindTagBtnShowLocation: SETTINGS.TAG.BIND_TAG_LOCATION.TOP_RIGHT,    autoHideWindow: false,    alwaysOnTop: true,    newVersionAlertMode: SETTINGS.UPDATER.HINT_MODE.TOAST,    enableAnimationEffects: true,    animationDuration: 350,    animationSpeedLevel: SETTINGS.THEME.ANIMATION.SPEED.NORMAL.key,    autoGoToLatestData: true,    tagListLocation: SETTINGS.TAG.TAG_LIST_LOCATION.TOP_LEFT,    displayThumbnailImage: true,    imageBasePath: '',    enableImageSave: true,    pluginPath: '',});// 当前编辑的配置export const currentConfig = reactive<Settings>({...originalConfig});// 配置相关export const originalShortcutKeys = reactive<ShortcutKeys>({    search: {        name: "搜索",        key: [            "ctrl",            "f"        ]    },    wakeUpRoutine: {        name: "唤醒程序",        key: [            "alt",            "c"        ]    },    quickPaste: {        name: "快速粘贴",        key: [            "ctrl",            "alt"        ]    }});// 当前编辑的配置export const currentShortcutKeys = reactive<ShortcutKeys>({...originalShortcutKeys});// 【替换全局热键】功能配置 start/** * 当前程序是否占用系统快捷键 */export const systemClipboardKeyOccupied = ref(false);/** * 系统剪贴板是否启用 */export const systemClipboardEnable = ref(false);/** * 系统剪贴板未启用的情况下，系统剪贴板快捷键是否被注册 */export const systemClipboardKeysRegistered = ref(false);/** * 当前程序是否为管理员启动 */export const isAdminStart = ref(false);/** * 替换全局热键加载状态 */export const replaceGlobalHotkeyLoading = ref(true);/** * 替换全局热键加载完成 */export const replaceGlobalHotkeyLoaded = ref(false);// 【替换全局热键】功能配置 end