<script setup lang="ts">// 菜单相关import {selectedKey} from "./composables/settingsComposable.ts";import GeneralSettings from "./components/GeneralSettings.vue";import ThemeSettings from "./components/ThemeSettings.vue";import UpdateSettings from "./components/UpdateSettings.vue";import StorageSettings from "./components/StorageSettings.vue";import ShortcutSettings from "./components/ShortcutSettings.vue";import {useLanguage} from "../../services/LanguageService.ts";import SettingsMenu from "./components/SettingsMenu.vue";import TitleBar from "../../components/TitleBar.vue";const {currentLanguage} = useLanguage();</script><template>  <div class="settings-container">    <titleBar        :title="currentLanguage.pages.settings.title"        :showCloseBtn="true"        :dev-tool="`settings`"    />    <div class="settings-content">      <!-- 左侧菜单 -->      <SettingsMenu/>      <!-- 右侧内容 -->      <div class="settings-form">        <!-- 通用设置 -->        <GeneralSettings v-if="selectedKey === 'general'"/>        <!-- 主题设置 -->        <ThemeSettings v-else-if="selectedKey === 'themes'"/>        <!-- 更新设置 -->        <UpdateSettings v-else-if="selectedKey === 'updater'"/>        <!-- 存储设置 -->        <StorageSettings v-else-if="selectedKey === 'storage'"/>        <!-- 快捷键设置 -->        <ShortcutSettings v-else-if="selectedKey === 'shortcut'"/>      </div>    </div>  </div></template><style scoped>.settings-container {  height: 100vh;  display: flex;  flex-direction: column;}.settings-content {  flex: 1;  display: flex;  overflow: hidden;}.settings-form {  flex: 1;  padding: 20px;  overflow-y: auto;  position: relative;}/*以下为子组件通用样式*/:deep(.settings-section) {  margin-bottom: 20px;  overflow-y: auto;}:deep(.settings-section h2) {  margin-bottom: 20px;  font-size: 16px;  font-weight: 500;}:deep(.form-item) {  display: flex;  align-items: center;  margin-bottom: 16px;  justify-content: space-between;}:deep(.label) {  width: 200px;  margin-right: 16px;}:deep(.select) {  width: 30%;}:deep(.input-number) {  width: 30%;}:deep(.line) {  margin-bottom: 16px;}:deep(.main-item) {  display: flex;  align-items: center;  justify-content: space-between;}:deep(.hint) {  display: flex;  font-size: 11px;  opacity: 0.5;  align-items: center;  justify-content: flex-start;  flex-direction: row;  color: var(--theme-text);}:deep(.hint-icon) {  width: 12px;  height: 12px;  margin-right: 4px;}:deep(.hint-text) {  width: 98%;}</style>