import {clipboardItems, onCopy, searchBoxState, toggleSearchBox} from "./ClipboardDataComposable.ts";import {getCurrentWebviewWindow} from "@tauri-apps/api/webviewWindow";import {listFixedStore} from "../../../store/fixed.ts";import {listen} from "@tauri-apps/api/event";import {openAboutWindow, openSettingsWindow} from "../../../utils/WindowUtil.ts";import UpdaterService from "../../../services/UpdaterService.ts";import {convertRegisterKeys, formatKeysDisplay} from "../../../utils/ShortcutKeysUtil.ts";import {isRegistered, register, unregister} from "@tauri-apps/plugin-global-shortcut";import {saveQuickPasteKeyAvailable, saveWakeUpRoutineKeyAvailable} from "../../../store/ShortcutKeyAvailableStatus.ts";import {error, info} from "@tauri-apps/plugin-log";import {sendNotification} from "@tauri-apps/plugin-notification";import {currentMonitor, cursorPosition, PhysicalPosition} from "@tauri-apps/api/window";import {ref} from "vue";import {getAutoHideWindow} from "../../../store/Settings.ts";import {getQuickPaste, getWakeUpRoutine} from "../../../store/ShortcutKeys.ts";import {invoke} from "@tauri-apps/api/core";import type {MessageApiInjection} from "naive-ui/es/message/src/MessageProvider";import {currentLanguage} from "../../../services/LanguageService.ts";const latestShowWindowTime = ref(0);// 监听当前窗口是否固定const listFixedListen = listFixedStore();// 自动隐藏窗口export const isAutoHideWindow = ref(false);// 快速粘贴快捷键export const quickPasteKeys = ref<string[]>([]);/** * 隐藏当前窗口 */export async function hideWindow() {    if (!listFixedListen.stateData()) {        // 失焦隐藏窗口、搜索框显示时，需要隐藏搜索框        if (searchBoxState.visible) {            toggleSearchBox();        }        // 窗口未固定，触发失焦，隐藏窗口        await getCurrentWebviewWindow().hide();        // 发送窗口已经隐藏消息给后端        await invoke('hide_win_msg');        console.log("已隐藏窗口，" + latestShowWindowTime.value + "，" + Date.now());    }}/** * 注册打开当前窗口快捷键 * @param shortcutKeys 快捷键 */async function registerOpenWindowKey(shortcutKeys: string) {    console.log("注册打开当前窗口快捷键: " + shortcutKeys)    await register(shortcutKeys, async (event) => {        // 只监听按键按下，松开忽略        if (event.state === 'Released') {            return;        }        const win = getCurrentWebviewWindow();        const visible = await win.isVisible();        if (!visible) {            const {x, y} = await cursorPosition();            // 获取窗口大小            const size = await win.outerSize();            const monitor = await currentMonitor();            // 计算窗口位置，确保不会超出屏幕边界            let posX = x;            let posY = y;            if (monitor) {                const {width: screenWidth, height: screenHeight} = monitor.size;                // 如果窗口右边界超出屏幕，则向左偏移                if (x + size.width > screenWidth) {                    posX = screenWidth - size.width;                }                // 如果窗口下边界超出屏幕，则向上偏移                if (y + size.height > screenHeight) {                    posY = screenHeight - size.height;                }            }            // 设置窗口位置并显示            await win.setPosition(new PhysicalPosition(posX, posY));            await win.show();            await win.setFocus();            window.focus();            document.body.focus();            latestShowWindowTime.value = Date.now();        } else {            await hideWindow();        }    });}/** * 打开当前窗口快捷键注册 */async function registerShortcutKeysOpenWindow() {    console.log("注册快捷键打开当前窗口")    const wakeUpRoutine = await getWakeUpRoutine();    if (wakeUpRoutine && wakeUpRoutine.key && wakeUpRoutine.key.length > 0) {        try {            const shortcutKeys = convertRegisterKeys(wakeUpRoutine.key);            const registered = await isRegistered(shortcutKeys);            if (registered) {                console.log("快捷键已经注册，注销后重新注注册", shortcutKeys)                await unregister(shortcutKeys);            }            await registerOpenWindowKey(shortcutKeys);            saveWakeUpRoutineKeyAvailable(true);        } catch            (ex) {            error("打开程序快捷键注册失败，" + ex);            console.error(ex);            sendNotification({                title: 'EasyPaste',                body: currentLanguage.value.pages.list.registerOpenWindowShortcutKeysErrorHint.replace("{keys}", formatKeysDisplay(wakeUpRoutine.key))            });            saveWakeUpRoutineKeyAvailable(false);        }    }}/** * 注册快捷键快速粘贴数据 */async function registerQuickPasteKey(shortcutKeys: string, number: number, message: MessageApiInjection) {    console.log("注册快捷键快速粘贴数据: " + shortcutKeys)    await register(shortcutKeys, async (event) => {        // 只监听按键按下，松开忽略        if (event.state === 'Released') {            return;        }        const item = clipboardItems.value[number - 1];        if (item) {            await onCopy(item, message);        }    })}/** * 注册快捷键快速粘贴数据 */async function registerShortcutKeysQuickPaste(message: MessageApiInjection) {    console.log("注册快捷键快速粘贴数据")    const quickPaste = await getQuickPaste();    if (quickPaste && quickPaste.key && quickPaste.key.length > 0) {        quickPasteKeys.value = quickPaste.key;        const registerFailedKeys = [];        for (let i = 1; i <= 9; i++) {            const keys = [...quickPaste.key, String(i)];            try {                const shortcutKeys = convertRegisterKeys(keys);                const registered = await isRegistered(shortcutKeys);                if (registered) {                    console.log("快捷键已经注册，注销后重新注注册", shortcutKeys)                    await unregister(shortcutKeys);                }                await registerQuickPasteKey(shortcutKeys, i, message);                saveQuickPasteKeyAvailable(i, true);            } catch (ex) {                error("快速粘贴快捷键注册失败，" + ex);                console.error(ex);                saveQuickPasteKeyAvailable(i, false);                registerFailedKeys.push(formatKeysDisplay(keys));            }        }        if (registerFailedKeys.length > 0) {            sendNotification({                title: 'EasyPaste',                body: currentLanguage.value.pages.list.registerQuickPasteShortcutKeysErrorHint.replace("{keys}", registerFailedKeys.join(","))            });        }    }}/** * 初始化窗口失焦定时任务 * todo tauri://blur 只会生效一次，暂时只能用这种笨办法 */let focusState = false; // 窗口聚焦状态let webFocusStatus = false; // web网页聚焦状态let blurTimer: any = null;function initBlurTimer() {    info("初始化窗口失焦定时任务");    return setInterval(async () => {        window.onfocus = () => {            webFocusStatus = true;        }        window.onblur = () => {            webFocusStatus = false;        }        const win = getCurrentWebviewWindow();        const focused = await win.isFocused();        if (!webFocusStatus && ((focusState && !focused) || (!focusState && !focused))) {            const visible = await win.isVisible();            if (visible) {                // console.log("触发窗口失焦，" + latestShowWindowTime.value + "，" + Date.now() + "，" + (latestShowWindowTime.value > Date.now() - 300));                // 如果上一次打开窗口的时间距离当前时间小于300毫秒，则忽略                if (latestShowWindowTime.value > Date.now() - 300) {                    return;                }                await hideWindow();            }        }        if (focused !== focusState) {            focusState = focused;        }    }, 200); // 时间可调整}/** * 初始化打开设置窗口事件监听 */let openSettingsListener: any = null;function initOpenSettingsListener() {    return listen('open-settings', (_event: any) => {        openSettingsWindow();    });}/** * 初始化打开关于窗口事件监听 */let openAboutListener: any = null;function initOpenAboutListener() {    return listen('open-about', (_event: any) => {        openAboutWindow();    });}/** * 初始化打开关于窗口事件监听 */let trayOpenWindowListener: any = null;function initTrayOpenWindowListener() {    return listen('tray-open-window', async (_event: any) => {        latestShowWindowTime.value = Date.now();        await getCurrentWebviewWindow().setFocus();        info("已触发托盘图标打开窗口" + latestShowWindowTime.value);    });}/** * 初始化手动检查更新事件监听 */let checkUpdateUnListener: any = null;async function initCheckUpdateListener() {    return await listen('check-update', async (_event: any) => {        const update = UpdaterService.getInstance();        await update.checkForUpdates(true);    });}/** * 初始化修改快捷键打开当前窗口监听 */let updateRegisterShortcutKeysOpenWindowListener: any = null;async function initUpdateRegisterShortcutKeysOpenWindowListener() {    return await listen('update-open-window-key', async (event: any) => {        const keys: ShortcutKeys = event.payload.keys;        if (keys.wakeUpRoutine && keys.wakeUpRoutine.key && keys.wakeUpRoutine.key.length > 0) {            const shortcutKeys = convertRegisterKeys(keys.wakeUpRoutine.key);            await registerOpenWindowKey(shortcutKeys);        }    });}/** * 初始化修改快捷键打开当前窗口监听 */let updateRegisterShortcutKeysQuickPasteListener: any = null;async function initUpdateRegisterShortcutKeysQuickPasteListener(message: MessageApiInjection) {    return await listen('update-quick-paste-key', async (event: any) => {        const keys: ShortcutKeys = event.payload.keys;        if (keys.quickPaste && keys.quickPaste.key && keys.quickPaste.key.length > 0) {            quickPasteKeys.value = keys.quickPaste.key;            for (let i = 1; i <= 9; i++) {                const keysArr = [...keys.quickPaste.key, String(i)];                try {                    const shortcutKeys = convertRegisterKeys(keysArr);                    const registered = await isRegistered(shortcutKeys);                    if (registered) {                        console.log("快捷键已经注册，注销后重新注注册", shortcutKeys)                        await unregister(shortcutKeys);                    }                    await registerQuickPasteKey(shortcutKeys, i, message);                } catch (ex) {                    error("快速粘贴快捷键注册失败，" + ex);                    console.error(ex);                }            }        }    });}/** * 初始化更新自动隐藏窗口监听 */let updateAutoHideWindowListener: any = null;async function initUpdateAutoHideWindowListener() {    return await listen('update-auto-hide-window', async (event: any) => {        const autoHideWindow = event.payload.isAutoHide;        console.log('更新自动隐藏窗口状态', autoHideWindow)        isAutoHideWindow.value = autoHideWindow;        if (autoHideWindow) {            if (blurTimer) {                clearInterval(blurTimer);            }            // 添加监听窗口失焦自动隐藏定时任务            blurTimer = initBlurTimer();        } else {            // 清除窗口失焦定时任务            if (blurTimer) {                clearInterval(blurTimer);            }        }    });}/** * 初始化更新窗口置顶状态监听 */let updateAlwaysOnTopListener: any = null;function initUpdateAlwaysOnTopListener() {    return listen('update-always-on-top', async (event: any) => {        const isTop = event.payload.isTop;        const window = getCurrentWebviewWindow();        await window.setAlwaysOnTop(isTop);    });}/** * 初始化窗口配置 */export async function initializeWindow(message: MessageApiInjection) {    try {        // 添加托盘图标打开窗口事件监听        trayOpenWindowListener = await initTrayOpenWindowListener();        // 添加监听窗口失焦自动隐藏定时任务        isAutoHideWindow.value = await getAutoHideWindow();        if (isAutoHideWindow.value) {            latestShowWindowTime.value = Date.now();            blurTimer = initBlurTimer();        }        // 添加打开设置窗口事件监听        openSettingsListener = await initOpenSettingsListener();        // 添加打开关于窗口事件监听        openAboutListener = await initOpenAboutListener();        // 添加检查更新事件监听        checkUpdateUnListener = await initCheckUpdateListener();        // 注册快捷键打开当前窗口        registerShortcutKeysOpenWindow()            .catch(e => {                error("注册快捷键打开当前窗口失败" + e);            });        // 注册快捷键快速粘贴        registerShortcutKeysQuickPaste(message)            .catch(e => {                error("注册快捷键快速粘贴失败" + e);            })        // 添加修改快捷键打开当前窗口事件监听        updateRegisterShortcutKeysOpenWindowListener = await initUpdateRegisterShortcutKeysOpenWindowListener();        // 添加修改快捷键快速粘贴事件监听        updateRegisterShortcutKeysQuickPasteListener = await initUpdateRegisterShortcutKeysQuickPasteListener(message);        // 添加更新自动隐藏窗口事件监听        updateAutoHideWindowListener = await initUpdateAutoHideWindowListener();        // 添加修改窗口置顶状态事件监听        updateAlwaysOnTopListener = await initUpdateAlwaysOnTopListener();    } catch (e) {        console.error("窗口配置初始化失败", e);        error("窗口配置初始化失败" + e);    }}/** * 销毁窗口配置 */export function destroyWindow() {    // 清除托盘图标打开窗口事件监听    if (trayOpenWindowListener) {        trayOpenWindowListener();    }    // 清除窗口失焦定时任务    if (blurTimer) {        clearInterval(blurTimer);    }    // 清除打开设置窗口事件监听    if (openSettingsListener) {        openSettingsListener();    }    // 清除打开关于窗口事件监听    if (openAboutListener) {        openAboutListener();    }    // 清除检查更新事件监听    if (checkUpdateUnListener) {        checkUpdateUnListener();    }    // 清除修改快捷键打开当前窗口事件监听    if (updateRegisterShortcutKeysOpenWindowListener) {        updateRegisterShortcutKeysOpenWindowListener();    }    // 清除修改快捷键快速粘贴事件监听    if (updateRegisterShortcutKeysQuickPasteListener) {        updateRegisterShortcutKeysQuickPasteListener();    }    // 添加更新自动隐藏窗口事件监听    if (updateAutoHideWindowListener) {        updateAutoHideWindowListener();    }    // 清除修改窗口置顶状态事件监听    if (updateAlwaysOnTopListener) {        updateAlwaysOnTopListener();    }}