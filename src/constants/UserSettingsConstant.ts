// 用户设置相关常量import {lightTheme} from "../data/themes/light.ts";import {chinese} from "../data/locales/zh.ts";export const SETTINGS = {    // 菜单常量    MENU: {        // 菜单项常量        OPTIONS: [            {key: 'general', index: 1, labelKey: 'generalMenu'},            {key: 'mw', index: 2, labelKey: 'mwMenu'},            {key: 'tags', index: 3, labelKey: 'tagMenu'},            {key: 'themes', index: 4, labelKey: 'themesMenu'},            {key: 'updater', index: 5, labelKey: 'updaterMenu'},            {key: 'storage', index: 6, labelKey: 'storageMenu'},            {key: 'shortcut', index: 7, labelKey: 'shortcutMenu'},        ],    },    // 标签设置页面常量    TAG: {        // 绑定标签按钮展示位置        BIND_TAG_LOCATION: {            TOP_RIGHT: 'top-right',            BOTTOM_RIGHT: 'bottom-right'        },        // 标签列表位置常量        TAG_LIST_LOCATION: {            TOP_LEFT: 'top-left',            BOTTOM_LEFT: 'bottom-left'        }    },    // 主题设置常量    THEME: {        // 默认主题        DEFAULT_THEME: lightTheme.id,        // 动画相关常量        ANIMATION: {            // 动画速度级别常量            SPEED: {                FAST: {key: 'fast', duration: 150},                NORMAL: {key: 'normal', duration: 350},                SLOW: {key: 'slow', duration: 500},            }        }    },    // 语言设置常量    LANGUAGE: {        // 默认语言        DEFAULT_LANGUAGE: chinese.id,    },    // 更新设置页面常量    UPDATER: {        // 检查更新方式常量        CHECK_MODE: {            TIMING: 'timing',            AFTER_RUNNING: 'after-running'        },        // 更新提示方式常量        HINT_MODE: {            TOAST: 'toast',            DIALOG: 'dialog'        }    }}/** * 获取动画速度级别对应的持续时间 * @param speedLevel 动画速度级别 */export function getSpeedDuration(speedLevel: string): number {    switch (speedLevel) {        case SETTINGS.THEME.ANIMATION.SPEED.FAST.key:            return SETTINGS.THEME.ANIMATION.SPEED.FAST.duration;        case SETTINGS.THEME.ANIMATION.SPEED.NORMAL.key:            return SETTINGS.THEME.ANIMATION.SPEED.NORMAL.duration;        case SETTINGS.THEME.ANIMATION.SPEED.SLOW.key:            return SETTINGS.THEME.ANIMATION.SPEED.SLOW.duration;        default:            return SETTINGS.THEME.ANIMATION.SPEED.NORMAL.duration;    }}